# 导入数据
music <- read.csv("music.csv")
# 查看基本信息
str(music)
summary(music)
# 方差分析
res <- aov(productivity ~ condition, data = music)
# 查看结果
summary(res)
# 方差分析
res <- aov(productivity ~ condition, data = music)
# 查看结果
summary(res)
# 方差分析
res <- aov(productivity ~ condition, data = music)
# 查看结果
summary(res)
# 进行Tukey's HSD测试
TukeyHSD(res)
no_music <- music[music$condition == "no_music"][["produtivity"]]
no_music <- music[music$condition == "no_music",][["produtivity"]]
# 方差分析
res <- aov(productivity ~ condition, data = music)
# 查看结果
summary(res)
# 进行Tukey's HSD测试
TukeyHSD(res)
no_music <- music[music$condition == "no_music",][["produtivity"]]
music_no_choice <- music[music$condition == "music_no_choice", ][["produtivity"]]
music_choice <- music[music$condition == "music_choice", ][["produtivity"]]
no_music <- music[music$condition == "no_music",][["productivity"]]
music_no_choice <- music[music$condition == "music_no_choice", ][["productivity"]]
music_choice <- music[music$condition == "music_choice", ][["productivity"]]
# 分组信息查看
no_music <- music[music$condition == "no_music",][["productivity"]]
music_no_choice <- music[music$condition == "music_no_choice", ][["productivity"]]
music_choice <- music[music$condition == "music_choice", ][["productivity"]]
summary(no_music)
summary(music_no_choice)
summary(music_choice)
# 分组信息查看
no_music <- music[music$condition == "no_music",][["productivity"]]
music_no_choice <- music[music$condition == "music_no_choice", ][["productivity"]]
music_choice <- music[music$condition == "music_choice", ][["productivity"]]
summary(no_music, title="2")
summary(music_no_choice)
summary(music_choice)
# 分组信息查看
no_music <- music[music$condition == "no_music",][["productivity"]]
music_no_choice <- music[music$condition == "music_no_choice", ][["productivity"]]
music_choice <- music[music$condition == "music_choice", ][["productivity"]]
summary(no_music)
summary(music_no_choice)
summary(music_choice)
music_choice
hist(music_choice)
hist(music_choice, freq = T)
hist(music_choice, freq = F)
hist(music_choice, freq = F, breaks = 10)
hist(music_choice, freq = F, breaks = 2)
hist(music_choice, freq = F, breaks = 5)
hist(music_choice, freq = F, breaks = 8)
hist(music_choice, freq = F, breaks = 7)
hist(music_choice, freq = F, breaks = 6)
hist(music_choice, freq = F, breaks = 5)
hist(no_music, freq = F, breaks = 5)
hist(music_no_choice, freq = F, breaks = 5)
hist(music_no_choice, freq = F, breaks = 4)
# music_choice hist
hist(music_choice, freq = F, breaks = 4)
# music_choice hist
hist(music_choice, freq = F, breaks = 4)
# music_choice hist
hist(music_choice, freq = F, breaks = 4, main = "music_choice")
# music_choice hist
hist(music_no_choice, freq = F, breaks = 4, main = "music_no_choice")
# no_music hist
hist(no_music, freq = F, breaks = 4, main = "no_music")
# 进行Tukey's HSD测试
TukeyHSD(res)
# 方差分析
res <- aov(productivity ~ condition, data = music)
# 查看结果
summary(res)
